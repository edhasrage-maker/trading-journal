import BreakRetestClient from '@/components/studies/BreakRetestClient'

export default function BreakRetestStudyPage() {
  return (
    <div className="max-w-6xl mx-auto">
      <BreakRetestClient />
    </div>
  )
}
